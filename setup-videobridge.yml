---
- name: Setup Videobridge
  hosts: meet_vb
  gather_facts: false
  roles:
    - proxmox-container
  tasks:
    - name: Installiere Pakete
      ansible.builtin.apt:
        name:
          - python-apt
          - htop
- name: Setup Videobridge
  hosts: meet_vb
  gather_facts: true
  roles:
    - ssh_keys
    - update_os
    - lxc_logrotate
    - geerlingguy.certbot
    - ucs-ip-managed-client
    - jitsi-videobridge
