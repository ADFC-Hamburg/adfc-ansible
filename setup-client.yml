- hosts: arbeitsplatz
  tasks:
  - name: Installiere Packete
    apt:
      name: "{{ packages }}"
    vars:
      packages:
      - emacs
      - libreoffice
      - libreoffice-l10n-de
      - ncdu
      - git
  - name: Erstelle /etc/issue
    template:
      src: issue
      dest: /etc/issue
    tags: aktiv