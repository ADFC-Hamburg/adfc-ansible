---
- name: restart dehydrated http
  systemd:
    name: dehydrated-http.service
    reload: true
    state: restarted
    enabled: true

- name: Domains abholen
  ansible.builtin.command:
    cmd: dehydrated --cron
