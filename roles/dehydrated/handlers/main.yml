- name: restart dehydrated http
  systemd:
    name: dehydrated-http.service
    daemon-reload: true
    state: restarted
    enabled: true

- name: Domains abholen
  command:
    cmd: "dehydrated --cron"