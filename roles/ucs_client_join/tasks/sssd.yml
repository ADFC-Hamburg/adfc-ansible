- name: Creeate sssd configuration
  ansible.builtin.template:
    src: sssd.conf.j2
    dest: /etc/sssd/sssd.conf
    mode: "0640"
    owner: root
    group: root
  notify:
    - Restart SSSD
    - PAM auth update
