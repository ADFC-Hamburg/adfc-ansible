- name: "Installiere Chromium"
  apt:
    name: "{{ packages }}"
  vars:
    packages:
     - chromium-browser
     - chromium-browser-l10n
     - chromium-codecs-ffmpeg
     - chromium-codecs-ffmpeg-extra
