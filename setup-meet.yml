- hosts: meet.adfc-intern.de
  gather_facts: no
  roles:
    - proxmox-container
  tasks:
    - name: Installiere Pakete
      apt:
        name:
          - python-apt
          - htop
    - name: Setup Meet Only
      include_tasks: setup-meet-only.yml
